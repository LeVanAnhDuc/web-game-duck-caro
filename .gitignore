
# Bí mật — `.env.example` là bản mẫu và ĐƯỢC commit; `.env` là bản thật và không bao giờ commit.
.env
.env.*
!.env.example

# Node / Next
/node_modules/
/.next/
/out/
/coverage/
*.tsbuildinfo
next-env.d.ts

# Playwright MCP artefacts (anh chup, snapshot) — khong thuoc repo
/.playwright-mcp/

# Playwright
/test-results/
/playwright-report/
/blob-report/

# --- Claude Code: theo doi config + skills, bo qua worktrees/doc-state ---
/.claude/*
!/.claude/CLAUDE.md
!/.claude/settings.local.json
!/.claude/skills/
!/.claude/scripts/
